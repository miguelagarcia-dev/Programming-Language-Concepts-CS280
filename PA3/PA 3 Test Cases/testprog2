PROGRAM circle;
	(*Illegal use of a constant name as a variable*)
const
   pi = 3.14 ;
VAR
	(*Variable declarations*)
	r, a, p, b : REAL; 
	i, j : INTEGER;	
BEGIN
	r := 8;
	a := 0;
	pi := 0;
	
	j := -1;
	IF ( r > 5) THEN
	  a := (3.14) + k * r;
	IF (a < 100) THEN
	    p := 2 * 3.14 * b; 
	(*Display the results*)
	WRITELN  ('The output results are ' , r, p, b);
END
                