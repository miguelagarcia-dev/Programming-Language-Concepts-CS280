program prog12;
const
	factor = 2.5;
var
	{Illegal use of program name as a variable.}
	r, a, p : real; 
	i, j : integer := 3;
	b : boolean;
	str : string := 'End of Program';	
begin
	p := factor * -(i * factor); 
	a := +(i/factor);
	
	prog12 := a + p ;
	writeln('The result of r = ' , r);
	writeln(str)
end.
